name: dilang_desktop
description: DiLang Flutter Desktop Application (Linux/Windows/macOS).
version: 2.0.0
publish_to: 'none'
resolution: workspace
homepage: https://github.com/dilang/dilang

environment:
  sdk: '^3.5.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dilang_core: ^1.0.0
  dilang_application: ^2.0.0
  dilang_storage: ^1.0.0
  dilang_learner: ^1.0.0
  dilang_language: ^1.0.0
  dilang_learning_engine: ^1.0.0
  dilang_conversation: ^2.0.0
  dilang_design_system: ^2.0.0
  dilang_product_ui: ^2.1.0
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0
