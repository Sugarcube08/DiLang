name: dilang_learner
description: Pure Dart Learner Domain package for DiLang. Manages evolving user knowledge graphs.
version: 1.0.0
publish_to: 'none'
resolution: workspace
homepage: https://github.com/dilang/dilang

environment:
  sdk: '^3.5.0'

dependencies:
  dilang_core: ^1.0.0
  dilang_language: ^1.0.0
  meta: ^1.11.0
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.24.0
