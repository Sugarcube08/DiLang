name: dilang_conversation
description: Pure Dart Conversation Engine for DiLang. Consumes AI Runtime & emits Domain Events.
version: 2.0.0
publish_to: 'none'
resolution: workspace
homepage: https://github.com/dilang/dilang

environment:
  sdk: '^3.5.0'

dependencies:
  dilang_core: ^1.0.0
  dilang_language: ^1.0.0
  dilang_ai_runtime: ^1.0.0
  dilang_learning_engine: ^1.0.0
  meta: ^1.11.0
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.24.0
