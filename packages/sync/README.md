# `dilang_sync`

Pure Dart Synchronization & Replication Engine package for DiLang. Handles local-first event serialization, LZ4 delta compression, vector clock conflict resolution, and snapshot management.
