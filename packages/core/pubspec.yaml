name: dilang_core
description: Pure Dart Core Kernel for DiLang Language Acquisition Platform. Independent of Flutter.
version: 1.0.0
publish_to: 'none'
resolution: workspace
homepage: https://github.com/dilang/dilang

environment:
  sdk: '^3.5.0'

dependencies:
  meta: ^1.11.0
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.24.0
